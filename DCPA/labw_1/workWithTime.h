#ifndef WORKWITHTIME_H
#define WORKWITHTIME_H

#endif // WORKWITHTIME_H
#include <ctime>
int getSec (time_t* difference = 0);
int getMin(time_t* difference = 0);
int getHour (time_t* difference = 0);
int getMs (time_t* difference = 0);
int getYDay (time_t* difference = 0);
int getWDay (time_t* difference = 0);
int getMonth (time_t* difference = 0);
int getYear (time_t* difference = 0);
